export interface loginForm {
    email: string,
    password: string
}

export interface registerForm {
    name:string,
    clinicName:string,
    email:string,
    password:string,
    confirmPassword:string
}